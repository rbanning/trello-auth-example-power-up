{
  "name": "trello-base-power-up",
  "version": "1.0.0",
  "description": "Basic Trello Power Up to be used as starting point for other power ups",
  "main": "index.js",
  "repository": "https://github.com/rbanning/trello-base-power-up.git",
  "author": "rbanning <rob@myhallpass.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc && parcel build src/html/*.html",
    "lint": "tslint -p ./",
    "lintES": "eslint -c .eslintrc.js --ext .ts ./src"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-interpolate-env": "^0.0.1",
    "parcel-plugin-static-files-copy": "^2.5.1"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.1.5"
  }
}
