{
  "name": "trello-auth-example-power-up",
  "version": "1.0.0",
  "description": "Trello Power Up that uses current user's creds to post a comment",
  "main": "index.js",
  "repository": "https://github.com/rbanning/trello-auth-example-power-up.git",
  "author": "rbanning <rob@myhallpass.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc && parcel build src/html/*.html",
    "lint": "tslint -p ./",
    "lintES": "eslint -c .eslintrc.js --ext .ts ./src"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-interpolate-env": "^0.0.1",
    "parcel-plugin-static-files-copy": "^2.5.1"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.1.5"
  }
}
